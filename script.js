//Used to change the text of the Login button to Logout
function logout(element){
    element.innerText="Logout"
}

//Used to remove the Add Definition button
function hide(element){
    element.remove();
}